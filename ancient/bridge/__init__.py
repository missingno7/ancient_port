"""Typed views: VM memory <-> named fields.  The ONE place DGROUP offsets live.

Seeds from docs/ancient_empires/exe_map.md (player block DS:072C..0740,
current room DS:BFBA, actor table DS:B3AE, ...) — each offset is re-verified
against this oracle before a view is added.
"""
